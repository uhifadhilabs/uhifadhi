/*
 * The throwaway application's entrypoint. A Flex-installed application has
 * one; this one does nothing, because every behaviour the core's pages need
 * is shipped by a bundle and registered as a Stimulus controller.
 */
