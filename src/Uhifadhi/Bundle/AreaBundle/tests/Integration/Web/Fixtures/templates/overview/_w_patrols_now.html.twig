{# A CONTRIBUTED CARD, written the way the contract documents one: rendered
   with `with_context: false`, everything in ONE map, this module's own
   figures under `by.patrols`. It reads nothing else, which is what makes it
   a test of the host rather than of itself. #}
<div class="c" data-w="patrols_now">
    <span class="tab">Out right now<span class="src">&middot; {{ by.patrols.out }} open</span></span>
    <span class="ao-by patrols"><i></i>patrols</span>
    <div class="use">{{ by.patrols.walked }} km walked today.</div>
</div>
