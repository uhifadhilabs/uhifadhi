{# WHAT A CONTRIBUTED SECTION ACTUALLY LOOKS LIKE: rows, and nothing around
   them. The card, the heading and the contributor tag are the area's, drawn
   once for every module — so this fixture writes exactly what a real module
   would and nothing more. #}
<div class="rln"><span class="fog">Expected shifts</span><span>{{ shifts }}</span></div>
