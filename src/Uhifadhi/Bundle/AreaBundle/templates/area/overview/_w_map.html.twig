{# WHERE — the area's own plate, and every module's layer over it.

   ONE PLATE, MANY OWNERS: the boundary and the zones are the area's and are
   always drawn; a module adds the data it owns as its own layer with its own
   key row. Infrastructure, so the cell is here whichever modules are on. #}
{{ include('@Area/area/overview/_map.html.twig') }}
